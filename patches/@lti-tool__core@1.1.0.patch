diff --git a/dist/ltiTool.js b/dist/ltiTool.js
index 0d01405..3cd7bcc 100644
--- a/dist/ltiTool.js
+++ b/dist/ltiTool.js
@@ -131,7 +131,8 @@ export class LTITool {
                 throw new Error('No issuer in token');
             }
             // 2. get the launchConfig so we can get the remote JWKS from our data store
-            const launchConfig = await getValidLaunchConfig(this.config.storage, unverified.iss, unverified.aud, unverified['https://purl.imsglobal.org/spec/lti/claim/deployment_id']);
+            const unverifiedAudiences = Array.isArray(unverified.aud) ? unverified.aud : [unverified.aud];
+            const launchConfig = await getValidLaunchConfig(this.config.storage, unverified.iss, unverifiedAudiences.filter((audience) => typeof audience === 'string'), unverified['https://purl.imsglobal.org/spec/lti/claim/deployment_id']);
             // 3. Verify LMS JWT
             const payload = await this.verifyLaunchJwtWithCachedJwks(validatedParams.idToken, launchConfig.jwksUrl);
             // 4. Verify our state JWT
@@ -139,8 +140,9 @@ export class LTITool {
             // 5. Parse and validate LMS JWT
             const validated = LTI13JwtPayloadSchema.parse(payload);
             // 6. Verify client id matches (audience claim)
-            if (validated.aud !== launchConfig.clientId) {
-                throw new Error(`Invalid client_id: expected ${launchConfig.clientId}, got ${validated.aud}`);
+            const validatedAudiences = Array.isArray(validated.aud) ? validated.aud : [validated.aud];
+            if (!validatedAudiences.includes(launchConfig.clientId)) {
+                throw new Error(`Invalid client_id: expected ${launchConfig.clientId}, got ${validatedAudiences.join(', ')}`);
             }
             // 7. Verify nonce matches
             if (stateData.nonce !== validated.nonce) {
diff --git a/dist/schemas/client.schema.js b/dist/schemas/client.schema.js
index efbc4a5..04a6413 100644
--- a/dist/schemas/client.schema.js
+++ b/dist/schemas/client.schema.js
@@ -1,5 +1,5 @@
 import * as z from 'zod';
-import { DeploymentSchema } from './deployment.schema';
+import { DeploymentSchema } from './deployment.schema.js';
 export const ClientSchema = z.object({
     id: z.uuid().describe('Internal stable UUID for the client'),
     name: z.string().min(1).describe('human-readable name for the platform'),
diff --git a/dist/schemas/lti13/dynamicRegistration/registrationResponse.schema.js b/dist/schemas/lti13/dynamicRegistration/registrationResponse.schema.js
index 3848f07..95a5775 100644
--- a/dist/schemas/lti13/dynamicRegistration/registrationResponse.schema.js
+++ b/dist/schemas/lti13/dynamicRegistration/registrationResponse.schema.js
@@ -1,5 +1,5 @@
 import * as z from 'zod';
-import { LTIMessagesArraySchema } from './ltiMessages.schema';
+import { LTIMessagesArraySchema } from './ltiMessages.schema.js';
 /**
  * Zod schema for LTI tool configuration within dynamic registration response.
  * Contains tool-specific metadata returned by the platform after successful registration.
diff --git a/dist/schemas/lti13/dynamicRegistration/toolRegistrationPayload.schema.js b/dist/schemas/lti13/dynamicRegistration/toolRegistrationPayload.schema.js
index 673f326..7dbc5a3 100644
--- a/dist/schemas/lti13/dynamicRegistration/toolRegistrationPayload.schema.js
+++ b/dist/schemas/lti13/dynamicRegistration/toolRegistrationPayload.schema.js
@@ -1,5 +1,5 @@
 import * as z from 'zod';
-import { LTIMessagesArraySchema } from './ltiMessages.schema';
+import { LTIMessagesArraySchema } from './ltiMessages.schema.js';
 /**
  * Zod schema for LTI tool configuration section within tool registration payload.
  * Contains LTI-specific metadata about the tool being registered with the platform.
diff --git a/dist/services/dynamicRegistrationHandlers/platform.js b/dist/services/dynamicRegistrationHandlers/platform.js
index 1d05761..13dce06 100644
--- a/dist/services/dynamicRegistrationHandlers/platform.js
+++ b/dist/services/dynamicRegistrationHandlers/platform.js
@@ -1,4 +1,4 @@
-import { RegistrationResponseSchema, } from '../../schemas';
+import { RegistrationResponseSchema, } from '../../schemas/index.js';
 import { escapeHtml } from '../../utils/htmlEscaping.js';
 import { getAGSScopes, hasAGSSupport, hasDeepLinkingSupport, hasNRPSSupport, } from '../../utils/ltiPlatformCapabilities.js';
 import { ltiServiceFetch } from '../../utils/ltiServiceFetch.js';
diff --git a/dist/utils/launchConfigValidation.d.ts b/dist/utils/launchConfigValidation.d.ts
index 3b0fa1c..dc6a735 100644
--- a/dist/utils/launchConfigValidation.d.ts
+++ b/dist/utils/launchConfigValidation.d.ts
@@ -1,3 +1,3 @@
 import type { LTILaunchConfig, LTIStorage } from '../interfaces/index.js';
-export declare function getValidLaunchConfig(storage: LTIStorage, iss: string, clientId: string, deploymentId: string): Promise<LTILaunchConfig>;
+export declare function getValidLaunchConfig(storage: LTIStorage, iss: string, clientId: string | readonly string[], deploymentId: string): Promise<LTILaunchConfig>;
 //# sourceMappingURL=launchConfigValidation.d.ts.map
\ No newline at end of file
diff --git a/dist/utils/launchConfigValidation.js b/dist/utils/launchConfigValidation.js
index e16ff5d..494f7f3 100644
--- a/dist/utils/launchConfigValidation.js
+++ b/dist/utils/launchConfigValidation.js
@@ -1,7 +1,13 @@
 export async function getValidLaunchConfig(storage, iss, clientId, deploymentId) {
-    const launchConfig = await storage.getLaunchConfig(iss, clientId, deploymentId);
-    if (!launchConfig) {
-        throw new Error('No valid launch config found');
+    const clientIds = Array.isArray(clientId) ? clientId : [clientId];
+    for (const candidateClientId of clientIds) {
+        if (typeof candidateClientId !== 'string' || candidateClientId.length === 0) {
+            continue;
+        }
+        const launchConfig = await storage.getLaunchConfig(iss, candidateClientId, deploymentId);
+        if (launchConfig) {
+            return launchConfig;
+        }
     }
-    return launchConfig;
+    throw new Error('No valid launch config found');
 }
diff --git a/dist/utils/ltiServiceFetch.js b/dist/utils/ltiServiceFetch.js
index a9b07fc..d4fe023 100644
--- a/dist/utils/ltiServiceFetch.js
+++ b/dist/utils/ltiServiceFetch.js
@@ -1,4 +1,4 @@
-import * as packageJson from '../../package.json';
+const packageVersion = '1.1.0';
 /**
  * Wrapper around fetch() that automatically adds User-Agent header for LTI service requests.
  *
@@ -25,7 +25,7 @@ export async function ltiServiceFetch(url, init) {
     const headers = new Headers(init?.headers);
     // Add User-Agent only if not already present (allows override)
     if (!headers.has('User-Agent')) {
-        headers.set('User-Agent', `lti-tool/${packageJson.version} (https://github.com/lti-tool/lti-tool)`);
+        headers.set('User-Agent', `lti-tool/${packageVersion} (https://github.com/lti-tool/lti-tool)`);
     }
     // Call fetch with merged headers
     return fetch(url, {
